import { SuperJsonPipe } from './super-json-pipe';

describe('SuperJsonPipe', () => {
  it('create an instance', () => {
    const pipe = new SuperJsonPipe();
    expect(pipe).toBeTruthy();
  });
});
